# =============================================================================
# RoboLearn Interface Environment Variables (.env.example)
# =============================================================================
# Copy this file to .env.local and fill in the values for local development.
# For production, set these in your CI/CD environment (e.g., GitHub Actions secrets).

# -----------------------------------------------------------------------------
# Authentication Server
# -----------------------------------------------------------------------------
# URL of the auth-server (Better Auth OIDC Provider)
AUTH_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# OAuth Configuration
# -----------------------------------------------------------------------------
# These are built into the code but documented here for reference:
# - Client ID: robolearn-interface (public client, uses PKCE)
# - OAuth callback: {SITE_URL}/auth/callback
# - Scopes: openid profile email

# -----------------------------------------------------------------------------
# Site Configuration
# -----------------------------------------------------------------------------
# Base URL of this interface (used for OAuth redirects)
# In development, this is auto-detected from window.location.origin
# In production, set this to your deployment URL
# SITE_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# Google Analytics 4 (Optional)
# -----------------------------------------------------------------------------
# Your GA4 measurement ID for analytics tracking.
# Leave empty to disable analytics.
# GA4_MEASUREMENT_ID=G-XXXXXXXXXX
